apiVersion: 1

providers:
  - name: smartgpu
    orgId: 1
    folder: SmartGPU
    type: file
    disableDeletion: false
    updateIntervalSeconds: 10
    options:
      path: /var/lib/grafana/dashboards
