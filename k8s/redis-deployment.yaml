apiVersion: apps/v1
kind: Deployment
metadata:
  name: smartgpu-redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: smartgpu-redis
  template:
    metadata:
      labels:
        app: smartgpu-redis
    spec:
      containers:
        - name: redis
          image: redis:7
          ports:
            - containerPort: 6379
